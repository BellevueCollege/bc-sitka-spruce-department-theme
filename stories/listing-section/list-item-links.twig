{%  if links|length > 0 %}
	<p>
		{% for link in links %}
			<a class="link-arrow" href="{{ link.url|esc_url }}">{{ link.title|e }}</a>
		{% endfor %}
	</p>
{% endif %}
{% if button %}
	<a class="btn btn-primary" href="{{ button.url|esc_url }}">{{ button.title|e }}</a>
{% endif %}
