{#
/**
 * @file
 * Bellevue template for a post.
 */
#}
{% extends 'system/wrapper.twig' %}

{% block content %}
      <div class="container-xl">
        <h1>{{ post.title }}</h1>
      </div>
      {{ post.content }}
{% endblock %}
