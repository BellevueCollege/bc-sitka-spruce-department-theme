# ============================================================
# azure-pipelines.yml — CI Pipeline
# Extends shared templates from bc-wordpress-devops-templates.
# ============================================================

trigger:
  branches:
    include:
      - trunk
  tags:
    include:
      - 'v*'
  paths:
    exclude:
      - azure-pipelines.yml
      - azure-pipelines-release.yml

pr:
  branches:
    include:
      - trunk
      - release-v*
      - hotfix-v*

resources:
  repositories:
    - repository: templates
      type: github
      name: BellevueCollege/bc-wordpress-devops-templates
      endpoint: github-service-connection

extends:
  template: templates/ci/theme-ci.yml@templates
  parameters:
    themeName: 'bc-sitka-spruce-department-theme'
    sshServiceConnectionTest: 'cstage'
    variableGroupTest: 'kinsta-cstage'
    buildSteps:
      - task: Npm@1
        inputs:
          command: 'custom'
          customCommand: 'run build'
        displayName: 'NPM Build'
      - task: replacetokens@6
        inputs:
          sources: 'style.css'
          addBOM: true
        displayName: 'Inject Version into style.css'
