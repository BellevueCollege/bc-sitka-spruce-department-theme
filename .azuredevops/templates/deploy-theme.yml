# ============================================================
# .azuredevops/templates/deploy-theme.yml
# Reusable deploy template. Expects the theme ZIP to already
# be downloaded by the calling pipeline before invoking.
#
# Requires variable group in scope with:
#   serverThemesPath, serverWpPath, serverBackupPath
# sshServiceConnection must be passed as a compile-time literal
# (Azure DevOps cannot resolve service connections from runtime macros).
# ============================================================

parameters:
  - name: sshServiceConnection
    type: string
  - name: zipSourceFolder
    type: string

steps:

  # Copy ZIP to server
  - task: CopyFilesOverSSH@0
    inputs:
      sshEndpoint: '${{ parameters.sshServiceConnection }}'
      sourceFolder: '${{ parameters.zipSourceFolder }}'
      contents: '$(themeName).zip'
      targetFolder: '$(serverThemesPath)'
      overwrite: true
      failOnEmptySource: true
    displayName: 'Copy ZIP to Server'

  # Backup existing theme, install new one via WP CLI
  - task: SSH@0
    inputs:
      sshEndpoint: '${{ parameters.sshServiceConnection }}'
      runOptions: inline
      inline: |
        set -e

        THEME_DIR="$(serverThemesPath)/$(themeName)"
        THEME_ZIP="$(serverThemesPath)/$(themeName).zip"
        WP_PATH="$(serverWpPath)"
        # $$(date) escapes Azure macro syntax so the shell receives $(date ...)
        BACKUP_FILE="$(serverBackupPath)/$(themeName)_$$(date +%Y%m%d_%H%M%S).zip"

        mkdir -p "$(serverBackupPath)"

        if [ -d "$THEME_DIR" ]; then
          echo "Backing up existing theme..."
          cd "$(dirname "$THEME_DIR")"
          zip -r "$BACKUP_FILE" "$(basename "$THEME_DIR")"
          echo "Backup saved to $BACKUP_FILE"
        else
          echo "No existing theme found — skipping backup."
        fi

        echo "Installing new theme..."
        wp theme install "$THEME_ZIP" \
          --force \
          --path="$WP_PATH" \
          --skip-plugins \
          --skip-themes

        wp theme is-installed "$(themeName)" --path="$WP_PATH"

        echo "Done."
    displayName: 'Backup and Install Theme'

  # Clean up ZIP from server (only runs if install succeeded)
  - task: SSH@0
    inputs:
      sshEndpoint: '${{ parameters.sshServiceConnection }}'
      runOptions: inline
      inline: |
        rm -f "$(serverThemesPath)/$(themeName).zip"
        echo "ZIP removed from server."
    displayName: 'Cleanup ZIP from Server'
    condition: succeeded()
